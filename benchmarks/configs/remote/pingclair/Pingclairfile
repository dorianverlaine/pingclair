# Benchmark config for Pingclair on the bare-metal Aliyun Shenzhen VPS.
# Same shape as benchmarks/configs/pingclair/Pingclairfile, adjusted for
# real filesystem paths instead of container paths.
#
# Named site address (`bench.local:8080`): the adapter binds hostname-based
# addresses on 0.0.0.0 and routes by the Host header; the benchmark scripts
# all send `Host: bench.local`.

global {
    debug false
}

bench.local:8080 {
    compress gzip

    handle /proxy/* {
        reverse_proxy 127.0.0.1:9000
    }

    file_server /root/bench/html
}
