{
	auto_https off
	admin off
}

http://bench.local:8080 {
	root * /var/www/html
	encode gzip

	handle /proxy/* {
		uri strip_prefix /proxy
		reverse_proxy backend:9000
	}

	handle {
		file_server
	}
}
