=== M1 production drill ===
image=pingclair:rc-8294116  host=ip-172-31-35-89.ap-east-2.compute.internal  aarch64  2026-07-28T01:33:07Z
Caddy keeps serving the tunnel throughout; Pingclair is a parallel container.

0. bring Pingclair up beside Caddy
  pingclair=172.18.0.5  caddy=172.18.0.3
  ✅ serves the production site over TLS on the custom port 6688

1. admin off
  ✅ no admin listener in the container (`admin off` honoured)

2. security headers — set and remove
  ✅ Strict-Transport-Security is set
  ✅ X-Content-Type-Options is set
  ✅ X-Frame-Options is set
  ✅ Referrer-Policy is set
  ✅ Content-Security-Policy is byte-identical to Caddy's
  ✅ `-Server` removed the Server header

3. three Cache-Control classes and `not path` AND semantics
  ✅ /api/* → no-store
  ✅ /assets/* → immutable
  ✅ / → no-cache (matched both negations)
  ✅ /api/ping is excluded from @rest
  ✅ /assets/nonexistent.js is excluded from @rest

4. compression negotiation
  ✅ Accept-Encoding: gzip, deflate, br, zstd → zstd
  ✅ Accept-Encoding: gzip, deflate → gzip
  ✅ Accept-Encoding: br → identity
  ✅ Accept-Encoding: identity → identity
  ✅ gzip body decodes byte-exact to what Caddy serves

5. verified client IP
  ✅ CF-Connecting-IP from the trusted tunnel subnet becomes the client IP

6. JSON access log and redaction
  ✅ access log lines are JSON
  ✅ no credential reached the log (query token, Cookie, Authorization, Referer code)

7. internal CA survives a restart
  ✅ internal CA root is the same authority after a restart
  ✅ leaf certificate is reused, not re-issued, after a restart

8. body equivalence with Caddy
  ✅ / body is byte-identical to Caddy's
  ✅ /api/ping body is byte-identical to Caddy's

9. HTTP/2 to the origin
  ✅ negotiates HTTP/2 over TLS (what the tunnel asks for)

10. graceful shutdown
  ✅ SIGTERM shutdown exited cleanly (code 0, no SIGKILL)

=== PASS ===
checks=27 failures=0
evidence: /tmp/m1drill
