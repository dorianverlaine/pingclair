// Basic Pingclairfile example
// A simple static file server with automatic HTTPS

server "localhost" {
    // Listen on HTTP and HTTPS
    // tls auto;  // Uncomment for production

    route "/" {
        file_server "./public" {
            index ["index.html", "index.htm"];
            browse false;
        }
    }
}
