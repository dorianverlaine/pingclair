# 🧪 The CI nextest profile: run every test even when one fails, and keep a
# JUnit report under the Cargo target directory for artifact inspection.
[profile.ci]
fail-fast = false
failure-output = "immediate-final"

[profile.ci.junit]
path = "junit.xml"
